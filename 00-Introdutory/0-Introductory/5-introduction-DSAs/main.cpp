// En C++ vamos a hacer DSAs, en C vamos a aprender a programar directo

// Datos primitivos son: -> enteros, flotantes (decimales), booleanos, strings
// Datos abstractos son: -> arreglos, vectores, matrices, listas anidadas, colas, pilas, grafos, etc...

// Un algoritmo es una receta de cocina para cumplir algo en espefico
//
// O(1), O(n), O(n^2)

#include <iostream>
#include <string>

using namespace std;

int main(void) {
  printf("Hello world\n");
}
